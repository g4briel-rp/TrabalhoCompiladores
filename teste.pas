program exs1;

var i: integer;
n1, n2, n3, n4: real;

begin;
    {write("n1: ");
    read(n1);
    write("n2: ");
    read(n2);
    write("n4: ");
    read(n4);
    n3 := n1 / (n2 * n4);
    write("resultado eh: ", n3);}

    for i:=1 to 11 do
    begin
        write(i);
    end;
end.